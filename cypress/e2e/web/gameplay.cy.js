describe('the play of the game', () => {
	it('should place a locked card for each AI player', () => {});
	it('should remove a card from each AI player', () => {});
	it('should place a Human card in the locked card', () => {});
	it('should remove the correct card from the Human deck', () => {});
	it('should deal cards to all but the Human upon a human win', () => {});
	it('should deal cards to the first AI player upon first AI player win', () => {});
	it('should declare a winner', () => {});
	it('should force game over if there is not a human interaction', () => {});
});