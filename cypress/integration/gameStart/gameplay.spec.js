import { StandardCard, StandardCardRanks, StandardCardSuits } from "@virtuoid/standard-card";
import Deck from "@virtuoid/deck";

describe('game play', () => {
	it('should start a round', () => {});
	it('should know when a round has completed', () => {});
	it('should continue a round if no winner is detected', () => {});
	it('should end the game if a winner is detected', () => {});
	it('should initiate a playoff if two or more winners are detected', () => {});
});