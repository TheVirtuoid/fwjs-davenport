import { StandardCard, StandardCardRanks, StandardCardSuits } from "@virtuoid/standard-card";
import Deck from "@virtuoid/deck";

describe('starting the game', () => {
	it('should create a standard deck of 52 cards', () => {});
	it('should shuffle the deck', () => {});
	it('should create an empty discard deck', () => {});
	it('should assign the appropriate values to the cards', () => {});
})